
record(libera, "$(P):$(B):gbe:reconnect") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).gbe.reconnect")
}

record(libera, "$(P):$(B):group_size") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).group_size")
}

record(libera, "$(P):$(B):gbe:status") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).gbe.status")
}

record(libera, "$(P):$(B):gbe:dest:mac") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).gbe.dest.mac")
}

record(libera, "$(P):$(B):fofb:reconnect") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.reconnect")
}

record(libera, "$(P):$(B):gbe:src:mac") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).gbe.src.mac")
}

record(libera, "$(P):$(B):fofb:pi:ki") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.pi.ki")
}

record(libera, "$(P):$(B):fofb:pi:kp") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.pi.kp")
}

record(libera, "$(P):$(B):fofb:pi:reset") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.pi.reset")
}

record(libera, "$(P):$(B):info:customer") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).info.customer")
}

record(libera, "$(P):$(B):fofb:dim:magnets") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.dim.magnets")
}

record(libera, "$(P):$(B):grouping:sfp:status3") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).grouping.sfp.status3")
}

record(libera, "$(P):$(B):grouping:sfp:status4") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).grouping.sfp.status4")
}

record(libera, "$(P):$(B):trig_delay") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).local_timing.trigger_delay")
}

record(libera, "$(P):$(B):fofb:switch") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.switch")
}

record(libera, "$(P):$(B):fofb:saturation:reset") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.saturation.reset")
}

record(libera, "$(P):$(B):events:trigger:timestamp") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).events.trigger.timestamp")
}

record(libera, "$(P):$(B):fofb:axis") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.axis")
}

record(libera, "$(P):$(B):gbe:dest:port") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).gbe.dest.port")
}

record(libera, "$(P):$(B):events:sync:count") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).events.sync.count")
}

record(libera, "$(P):$(B):info:health_status") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).info.health_status")
}

record(libera, "$(P):$(B):grouping:timeout") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).grouping.timeout")
}

record(libera, "$(P):$(B):gbe:dest:ip") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).gbe.dest.ip")
}

record(libera, "$(P):$(B):events:ilk:timestamp") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).events.interlock.timestamp")
}

record(libera, "$(P):$(B):fofb:su") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.su")
}

record(libera, "$(P):$(B):sync") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).local_timing.synchronize_lmt")
}

record(libera, "$(P):$(B):fofb:v") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.v")
}

record(libera, "$(P):$(B):info:revision") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).info.revision")
}

record(libera, "$(P):$(B):gbe:enable") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).gbe.enable")
}

record(libera, "$(P):$(B):events:current_time") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).events.current_time")
}

record(libera, "$(P):$(B):gbe:src:port") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).gbe.src.port")
}

record(libera, "$(P):$(B):events:trigger:count") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).events.trigger.count")
}

record(libera, "$(P):$(B):events:sync:timestamp") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).events.sync.timestamp")
}

record(libera, "$(P):$(B):gbe:src:ip") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).gbe.src.ip")
}

record(libera, "$(P):$(B):events:ilk:count") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).events.interlock.count")
}

record(libera, "$(P):$(B):fofb:dim:eigenmodes") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.dim.eigenmodes")
}

record(libera, "$(P):$(B):fofb:golden_orbit") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.golden_orbit")
}

record(libera, "$(P):$(B):sync_st_m") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).local_timing.sync_state_machine")
}

record(libera, "$(P):$(B):fofb:magnet_id") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.magnet_id")
}

record(libera, "$(P):$(B):fofb:saturation") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.saturation")
}

record(libera, "$(P):$(B):fofb:dim:bpms") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).fofb.dim.bpms")
}

record(liberaSignal, "$(P):$(B):event") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).signals.event")
}

record(liberaSignal, "$(P):$(B):fa") 
{
    info(MCII,  "mcii://$(D)/boards.$(MB).signals.fa")
    field(NGRP, 10000)
}
