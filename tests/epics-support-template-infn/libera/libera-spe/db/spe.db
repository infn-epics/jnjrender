
record(libera, "$(P):application:reset_counters") 
{
    info(MCII,  "mcii://$(D)/application.reset_counters")
}

record(libera, "$(P):spe:app-name") 
{
    info(MCII,  "mcii://$(D)/app-name")
}

record(libera, "$(P):spe:version") 
{
    info(MCII,  "mcii://$(D)/version")
}

record(libera, "$(P):application:clock_info:adc_frequency") 
{
    info(MCII,  "mcii://$(D)/application.clock_info.adc_frequency")
}

record(libera, "$(P):application:synchronize") 
{
    info(MCII,  "mcii://$(D)/application.synchronize_lmt")
}
